<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.admin.mapper.SysLogMapper">

    <insert id="insert" parameterType="com.admin.entity.SysLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_log (
            username, module, action, method, params, ip, execute_time, create_time
        ) VALUES (
            #{username}, #{module}, #{action}, #{method}, #{params}, #{ip}, #{executeTime}, NOW()
        )
    </insert>

</mapper>