<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admin.mapper.SysRoleMapper">

    <resultMap id="RoleResultMap" type="com.admin.entity.SysRole">
        <id column="id" property="id" />
        <result column="role_name" property="roleName" />
        <result column="role_code" property="roleCode" />
        <result column="description" property="description" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <select id="selectRolesByUserId" resultMap="RoleResultMap">
        SELECT r.*
        FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

    <select id="selectRoleCodesByUserId" resultType="string">
        SELECT r.role_code
        FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

    <select id="listAllRoles" resultMap="RoleResultMap">
        SELECT id, role_name, role_code, description, create_time, update_time
        FROM sys_role
        ORDER BY id ASC
    </select>

    <insert id="insert" parameterType="com.admin.entity.SysRole">
        INSERT INTO sys_role (role_name, role_code, description)
        VALUES (#{roleName}, #{roleCode}, #{description})
    </insert>

    <update id="update" parameterType="com.admin.entity.SysRole">
        UPDATE sys_role
        <set>
            <if test="roleName != null">role_name = #{roleName},</if>
            <if test="description != null">description = #{description},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="RoleResultMap">
        SELECT * FROM sys_role WHERE id = #{id}
    </select>

    <delete id="deleteById">
        DELETE FROM sys_role WHERE id = #{id}
    </delete>

</mapper>